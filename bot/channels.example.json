{
  "_comment": "Map each Discord channel ID to exactly {hostId, workDir}. hostId must exist in HOST_TOKENS; workDir must be a fully-qualified path native to that daemon host. Copy to channels.json and fill in real IDs.",
  "111111111111111111": { "hostId": "prod-1", "workDir": "C:/projects/foo" },
  "222222222222222222": { "hostId": "prod-2", "workDir": "/srv/apps/bar" }
}
